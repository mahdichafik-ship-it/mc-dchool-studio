import { useEffect, useMemo, useState } from "react";
import {
  ArrowLeft,
  ArrowRight,
  Camera,
  Check,
  CheckCircle,
  ChevronRight,
  CloudUpload,
  Crop,
  Expand,
  Keyboard,
  Pause,
  Play,
  QrCode,
  RotateCcw,
  RotateCw,
  Search,
  ShieldCheck,
  SlidersHorizontal,
  Star,
  Upload,
  Wifi,
  X,
} from "lucide-react";
import "./_group.css";

type Student = { id: number; firstName: string; lastName: string; studentId: string; captures: number; uploaded?: boolean };
type Capture = { id: number; frame: string; time: string; rating: number; pairing: string; upload: "Synced" | "Queued" | "Error"; image: string };

const students: Student[] = [
  { id: 1, firstName: "Maya", lastName: "Chen", studentId: "LHS-0247", captures: 4, uploaded: true },
  { id: 2, firstName: "Jordan", lastName: "Williams", studentId: "LHS-0248", captures: 0 },
  { id: 3, firstName: "Sofia", lastName: "Martinez", studentId: "LHS-0249", captures: 2, uploaded: true },
  { id: 4, firstName: "Ethan", lastName: "Nguyen", studentId: "LHS-0250", captures: 1 },
  { id: 5, firstName: "Aaliyah", lastName: "Johnson", studentId: "LHS-0251", captures: 0 },
  { id: 6, firstName: "Liam", lastName: "O'Connor", studentId: "LHS-0252", captures: 3, uploaded: true },
  { id: 7, firstName: "Grace", lastName: "Park", studentId: "LHS-0253", captures: 0 },
];
const captures: Capture[] = [
  { id: 1, frame: "001", time: "14:21:48", rating: 3, pairing: "JPEG + RAW", upload: "Synced", image: "/mc-school-studio-deck/hero.jpg" },
  { id: 2, frame: "002", time: "14:22:05", rating: 4, pairing: "JPEG + RAW", upload: "Synced", image: "/mc-school-studio-deck/hero.jpg" },
  { id: 3, frame: "003", time: "14:23:12", rating: 5, pairing: "JPEG only", upload: "Queued", image: "/mc-school-studio-deck/hero.jpg" },
  { id: 4, frame: "004", time: "14:24:08", rating: 0, pairing: "Needs review", upload: "Error", image: "/mc-school-studio-deck/hero.jpg" },
];

function Status({ status }: { status: Capture["upload"] }) {
  const color = status === "Synced" ? "text-emerald-600" : status === "Queued" ? "text-amber-600" : "text-rose-600";
  return <span className={`flex items-center gap-1 text-[10px] font-extrabold uppercase tracking-wider ${color}`}><CheckCircle className="size-3.5" />{status === "Error" ? "Upload error" : status}</span>;
}
function Stars({ value }: { value: number }) {
  return <span className="flex gap-0.5">{Array.from({ length: 5 }, (_, i) => <Star key={i} className="size-3.5 text-amber-400" fill={i < value ? "currentColor" : "none"} />)}</span>;
}

export function DesktopWithChanges() {
  const [selectedId, setSelectedId] = useState(1);
  const [reviewIndex, setReviewIndex] = useState(3);
  const [search, setSearch] = useState("");
  const [live, setLive] = useState(true);
  const [liveUpload, setLiveUpload] = useState(true);
  const [starred, setStarred] = useState<number[]>([2]);
  const [editorOpen, setEditorOpen] = useState(false);
  const [qrOpen, setQrOpen] = useState(false);
  const [quickLook, setQuickLook] = useState<Capture | null>(null);
  const [compareOpen, setCompareOpen] = useState(false);
  const [saved, setSaved] = useState(false);
  const [angle, setAngle] = useState(0);
  const [ratio, setRatio] = useState("Original");
  const selected = students.find((student) => student.id === selectedId) ?? students[0];
  const current = captures[reviewIndex];
  const visibleStudents = useMemo(() => students.filter((s) => `${s.firstName} ${s.lastName} ${s.studentId}`.toLowerCase().includes(search.toLowerCase())), [search]);
  const navigate = (direction: number) => setReviewIndex((value) => Math.max(0, Math.min(captures.length - 1, value + direction)));

  useEffect(() => {
    const onKey = (event: KeyboardEvent) => {
      if ((event.target as HTMLElement).tagName === "INPUT") return;
      if (event.key === "ArrowLeft") navigate(-1);
      if (event.key === "ArrowRight") navigate(1);
       if (event.key === "Escape") { setEditorOpen(false); setQrOpen(false); setQuickLook(null); setCompareOpen(false); }
    };
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  });

  return (
    <main className="capture-preview-redesign flex min-h-[720px] h-screen w-full flex-col overflow-hidden bg-slate-50 text-slate-900">
      <header className="z-20 flex shrink-0 items-center justify-between gap-4 border-b border-slate-800 bg-slate-950 px-5 py-3 text-white">
        <div className="flex min-w-0 items-center gap-4"><button aria-label="Back to projects" className="rounded-md bg-slate-900 p-2 text-slate-400 hover:text-white"><ArrowLeft className="size-4" /></button><div className="min-w-0"><h1 className="truncate text-sm font-extrabold">Lincoln Heights School</h1><p className="mt-1 text-[10px] font-bold uppercase tracking-[.18em] text-slate-500">Portrait day · Grade 08 · 128 students</p></div></div>
        <div className="flex shrink-0 items-center gap-2"><div className="hidden items-center gap-2 rounded-md border border-emerald-500/20 bg-emerald-500/10 px-3 py-2 text-[10px] font-bold uppercase tracking-wider text-emerald-300 sm:flex"><ShieldCheck className="size-3.5" />42 safe locally</div><button onClick={() => setLiveUpload(!liveUpload)} className={`flex items-center gap-1.5 rounded-md border px-3 py-2 text-[10px] font-bold uppercase tracking-wider ${liveUpload ? "border-teal-400/30 bg-teal-400/10 text-teal-300" : "border-slate-700 bg-slate-900 text-slate-400"}`}><CloudUpload className="size-3.5" />Live upload {liveUpload ? "on" : "off"}</button><button onClick={() => setLive(!live)} className="flex items-center gap-1.5 rounded-md border border-slate-700 bg-slate-900 px-3 py-2 text-[10px] font-bold uppercase tracking-wider text-slate-300">{live ? <Pause className="size-3" /> : <Play className="size-3" />}{live ? "Pause" : "Resume"}</button><button className="hidden items-center gap-1.5 rounded-md bg-blue-600 px-3 py-2 text-[10px] font-bold uppercase tracking-wider md:flex"><Upload className="size-3" />Finish shoot</button></div>
      </header>
      <div className="flex min-h-0 flex-1 overflow-hidden">
        <aside className="z-10 flex w-[300px] shrink-0 flex-col border-r border-slate-200 bg-white">
          <div className="border-b border-slate-100 px-4 py-3"><div className="mb-3 flex items-center justify-between"><span className="text-[10px] font-extrabold uppercase tracking-[.18em] text-slate-400">Capture target</span><span className="flex items-center gap-1 text-[10px] font-bold text-teal-700"><span className="size-1.5 animate-pulse rounded-full bg-teal-500" />ready</span></div><div className="relative"><Search className="absolute left-3 top-2.5 size-4 text-slate-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search roster..." className="w-full rounded-lg border border-slate-200 bg-slate-50 py-2 pl-9 pr-3 text-sm font-medium outline-none focus:border-teal-500" /></div></div>
          <div className="flex items-center justify-between border-b border-slate-100 bg-slate-50 px-4 py-2"><span className="text-[10px] font-extrabold uppercase tracking-widest text-slate-400">Grade 08 · All</span><span className="rounded bg-slate-200 px-1.5 py-0.5 text-[10px] font-bold text-slate-600">32</span></div>
          <div className="cp-scrollbar min-h-0 flex-1 overflow-y-auto py-2"><div className="px-4 py-2 text-[10px] font-extrabold uppercase tracking-widest text-slate-400">Students</div>{visibleStudents.map((student) => <button key={student.id} onClick={() => setSelectedId(student.id)} className={`cp-roster-row flex w-full items-center gap-3 border-b border-slate-100 border-l-4 p-3 text-left ${selectedId === student.id ? "is-active border-l-teal-500" : "border-l-transparent"}`}><div className={`flex size-8 shrink-0 items-center justify-center rounded-full text-xs font-extrabold ${selectedId === student.id ? "bg-teal-600 text-white" : "bg-slate-100 text-slate-500"}`}>{student.firstName[0]}{student.lastName[0]}</div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><span className="truncate text-sm font-bold">{student.lastName}, {student.firstName}</span>{selectedId === student.id ? <Camera className="size-3.5 text-teal-600" /> : <span className="rounded bg-slate-200 px-1.5 text-[10px] font-bold text-slate-600">{student.captures}</span>}</div><div className="mt-1 flex items-center justify-between"><span className="font-mono text-[10px] text-slate-500">{student.studentId}</span>{student.uploaded && <Check className="size-3 text-emerald-500" />}</div></div></button>)}</div>
          <div className="border-t border-slate-200 bg-slate-50 px-4 py-3 text-[10px] font-medium leading-relaxed text-slate-500"><div className="mb-1 flex items-center gap-1.5 font-bold text-slate-600"><Keyboard className="size-3.5" />Roster shortcuts</div><span className="font-mono text-slate-700">↑ ↓</span> navigate · <span className="font-mono text-slate-700">Esc</span> clear</div>
        </aside>
        <section className="cp-scrollbar min-w-0 flex-1 overflow-y-auto">
          <div className="flex items-center justify-between border-b border-slate-200 bg-white px-6 py-4"><div><div className="mb-1 flex items-center gap-2"><span className="flex items-center gap-1.5 rounded bg-teal-500 px-2 py-1 text-[10px] font-extrabold uppercase tracking-widest text-white"><Camera className="size-3" />Active target</span><span className="font-mono text-[10px] font-bold text-slate-500">{selected.studentId}</span></div><h2 className="text-xl font-extrabold tracking-tight">{selected.firstName} {selected.lastName}</h2></div><button onClick={() => setSelectedId(1)} className="rounded-md border border-slate-200 px-3 py-2 text-[10px] font-extrabold uppercase tracking-wider text-slate-500">Clear target</button></div>
          <div className="mx-auto flex max-w-[1050px] flex-col gap-4 p-5 lg:p-6">
            <div className="flex items-center justify-between"><div><p className="text-[10px] font-extrabold uppercase tracking-[.2em] text-slate-400">Visual confirmation</p><p className="mt-1 text-xs font-medium text-slate-500">Following the latest capture automatically</p></div><div className="flex items-center gap-2"><span className="hidden text-[10px] font-bold uppercase tracking-wider text-slate-400 sm:block">Capture review</span><button onClick={() => setEditorOpen(true)} className="flex items-center gap-2 rounded-lg border border-teal-600 bg-white px-3 py-2 text-[10px] font-extrabold uppercase tracking-wider text-teal-700 hover:bg-teal-50"><Crop className="size-3.5" />Edit framing</button></div></div>
            <div className="relative overflow-hidden rounded-2xl border border-slate-700 bg-slate-950 shadow-xl"><div className="absolute inset-x-0 top-0 z-10 flex items-center justify-between bg-gradient-to-b from-black/80 to-transparent p-4"><div className="flex items-center gap-2"><span className="rounded bg-red-600 px-2 py-1 text-[9px] font-extrabold uppercase tracking-widest text-white">Latest capture</span><span className="font-mono text-[10px] text-white/75">MAYA_0247_{current.frame}.JPG</span></div><span className="hidden items-center gap-1.5 text-[10px] font-bold uppercase tracking-wider text-white/65 sm:flex"><Wifi className="size-3" />JPEG preview</span></div><img key={current.id} src={current.image} alt={`Capture ${current.frame} of ${selected.firstName} ${selected.lastName}`} className="cp-preview-image aspect-[16/8.2] max-h-[330px] w-full object-cover object-center" /><button onClick={() => navigate(-1)} disabled={reviewIndex === 0} aria-label="Previous capture" className="absolute left-3 top-1/2 flex size-9 -translate-y-1/2 items-center justify-center rounded-full bg-black/45 text-white disabled:opacity-30"><ArrowLeft className="size-4" /></button><button onClick={() => navigate(1)} disabled={reviewIndex === captures.length - 1} aria-label="Next capture" className="absolute right-3 top-1/2 flex size-9 -translate-y-1/2 items-center justify-center rounded-full bg-black/45 text-white disabled:opacity-30"><ArrowRight className="size-4" /></button><div className="absolute bottom-0 inset-x-0 flex items-end justify-between bg-gradient-to-t from-black/80 to-transparent p-4 pt-10"><span className="font-mono text-[10px] text-white/75">{current.time} · frame {current.frame} / {captures.length}</span><span className="text-[10px] font-bold uppercase tracking-wider text-white/70">Lightweight JPEG</span></div></div>
            <div className="grid grid-cols-[1fr_250px] gap-3">
              <div className="grid grid-cols-3 gap-3 rounded-xl border border-slate-200 bg-white p-3"><div><p className="text-[9px] font-extrabold uppercase tracking-widest text-slate-400">Rating</p><button className="mt-2" onClick={() => setStarred(starred.includes(current.id) ? starred.filter((id) => id !== current.id) : [...starred, current.id])}><Stars value={current.rating} /></button></div><div><p className="text-[9px] font-extrabold uppercase tracking-widest text-slate-400">Pairing</p><p className={`mt-2 text-xs font-bold ${current.pairing === "Needs review" ? "text-rose-600" : "text-slate-700"}`}>{current.pairing}</p></div><div><p className="text-[9px] font-extrabold uppercase tracking-widest text-slate-400">Upload</p><div className="mt-2"><Status status={current.upload} /></div></div></div>
              <button onClick={() => setQrOpen(true)} className="flex items-center gap-3 rounded-xl border border-teal-200 bg-teal-50 p-3 text-left hover:border-teal-400"><div className="flex size-[54px] shrink-0 items-center justify-center rounded-lg border-4 border-slate-900 bg-white"><QrCode className="size-10 text-slate-900" /></div><div className="min-w-0"><p className="text-[9px] font-extrabold uppercase tracking-widest text-teal-700">Pairing QR</p><p className="mt-1 truncate text-xs font-extrabold text-slate-800">{selected.firstName} {selected.lastName}</p><span className="mt-1 flex items-center gap-1 text-[9px] font-bold uppercase tracking-wider text-teal-700"><Expand className="size-3" />Enlarge</span></div></button>
            </div>
             <div className="rounded-xl border border-slate-200 bg-white p-3"><div className="mb-3 flex items-center justify-between"><div><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-slate-500">Recent captures</p><p className="mt-1 text-[11px] text-slate-400">Newest on the right · click to review without changing target</p></div><span className="rounded-full bg-teal-50 px-2 py-1 text-[9px] font-extrabold uppercase tracking-wider text-teal-700">4 JPEG previews</span></div><div className="cp-scrollbar flex gap-2 overflow-x-auto pb-1">{captures.map((capture) => <button key={capture.id} onClick={() => setReviewIndex(captures.indexOf(capture))} className={`group relative min-w-[150px] overflow-hidden rounded-lg border-2 text-left ${current.id === capture.id ? "border-teal-500" : "border-slate-200"}`}><div className="relative aspect-[1.45] overflow-hidden bg-slate-900"><img src={capture.image} alt={`Capture ${capture.frame}`} className="h-full w-full object-cover" /><span className={`absolute left-2 top-2 rounded px-1.5 py-0.5 text-[8px] font-extrabold uppercase tracking-wider text-white ${capture.id === 4 ? "bg-red-600" : "bg-black/60"}`}>{capture.id === 4 ? "NEWEST" : `FRAME ${capture.frame}`}</span>{starred.includes(capture.id) && <Star className="absolute right-2 top-2 size-3.5 text-amber-300" fill="currentColor" />}<span onClick={(event) => { event.stopPropagation(); setQuickLook(capture); setCompareOpen(false); }} role="button" tabIndex={0} aria-label={`Quick look at capture ${capture.frame}`} className="absolute bottom-2 right-2 flex size-8 items-center justify-center rounded-md border border-white/40 bg-slate-950/75 text-white opacity-0 shadow-lg transition-opacity group-hover:opacity-100 group-focus-within:opacity-100"><Search className="size-4" /></span></div><div className="flex items-center justify-between gap-2 bg-white px-2 py-2"><span className="font-mono text-[9px] text-slate-500">{capture.time}</span><Status status={capture.upload} /></div></button>)}</div><p className="mt-2 flex items-center gap-1.5 text-[9px] font-bold uppercase tracking-wider text-slate-400"><Search className="size-3" />Hover a thumbnail and select the magnify control for Quick Look</p></div>
          </div>
        </section>
      </div>
      {quickLook && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/80 p-5" role="dialog" aria-modal="true" aria-label="Quick Look capture preview"><div className="flex max-h-[calc(100vh-40px)] w-full max-w-[1060px] flex-col overflow-hidden rounded-2xl border border-slate-700 bg-slate-900 shadow-2xl"><header className="flex items-center justify-between border-b border-white/10 px-5 py-3 text-white"><div><p className="text-[9px] font-extrabold uppercase tracking-[.2em] text-teal-300">Quick Look · workspace preview</p><h3 className="mt-1 text-sm font-extrabold">Frame {quickLook.frame} <span className="font-mono text-[10px] font-medium text-slate-400">· {quickLook.time}</span></h3></div><div className="flex items-center gap-2"><button onClick={() => setCompareOpen(!compareOpen)} className={`rounded-md border px-3 py-2 text-[9px] font-extrabold uppercase tracking-wider ${compareOpen ? "border-teal-400 bg-teal-400/15 text-teal-200" : "border-slate-600 text-slate-300 hover:border-teal-400"}`}>{compareOpen ? "Hide latest compare" : "Compare with latest"}</button><button onClick={() => { setQuickLook(null); setCompareOpen(false); }} aria-label="Close Quick Look" className="rounded-md p-2 text-slate-400 hover:bg-white/10 hover:text-white"><X className="size-5" /></button></div></header><div className={`grid min-h-0 flex-1 gap-3 bg-slate-950 p-4 ${compareOpen ? "md:grid-cols-2" : "grid-cols-1"}`}><div className="relative flex min-h-[300px] items-center justify-center overflow-hidden rounded-lg border border-teal-400/50 bg-slate-900"><img src={quickLook.image} alt={`Expanded capture ${quickLook.frame}`} className="max-h-[58vh] w-full object-contain" /><span className="absolute bottom-3 left-3 rounded bg-slate-950/80 px-2 py-1 font-mono text-[9px] text-white/75">FRAME {quickLook.frame} · JPEG PREVIEW</span><span className="absolute right-3 top-3 rounded bg-teal-400 px-2 py-1 text-[9px] font-extrabold uppercase tracking-wider text-slate-950">Inspecting</span></div>{compareOpen && <div className="relative flex min-h-[300px] items-center justify-center overflow-hidden rounded-lg border border-slate-700 bg-slate-900"><img src={current.image} alt={`Latest capture ${current.frame} for comparison`} className="max-h-[58vh] w-full object-contain opacity-90" /><span className="absolute bottom-3 left-3 rounded bg-slate-950/80 px-2 py-1 font-mono text-[9px] text-white/75">LATEST · FRAME {current.frame}</span><span className="absolute right-3 top-3 rounded bg-white/15 px-2 py-1 text-[9px] font-extrabold uppercase tracking-wider text-white">Current choice</span></div>}</div><footer className="flex items-center justify-between border-t border-white/10 px-5 py-3 text-[10px] text-slate-400"><span>Review only · active target remains {selected.firstName} {selected.lastName}</span><span className="font-mono text-slate-500">Esc close</span></footer></div></div>}
      {qrOpen && <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/80 p-5" role="dialog" aria-modal="true"><div className="relative w-full max-w-[480px] rounded-2xl bg-white p-6 text-center shadow-2xl"><button onClick={() => setQrOpen(false)} aria-label="Close QR code" className="absolute right-4 top-4 rounded p-1 text-slate-400 hover:bg-slate-100"><X className="size-5" /></button><p className="text-[10px] font-extrabold uppercase tracking-[.2em] text-teal-700">Pairing QR · selected target</p><h3 className="mt-2 text-2xl font-extrabold">{selected.firstName} {selected.lastName}</h3><div className="mx-auto my-5 flex size-[320px] max-h-[52vh] max-w-full items-center justify-center rounded-xl border-[16px] border-slate-950 bg-white"><QrCode className="size-full text-slate-950" /></div><p className="font-mono text-sm font-bold text-slate-600">{selected.studentId}</p><p className="mt-1 text-xs text-slate-500">Photograph or scan this code to pair the next capture</p><button onClick={() => setQrOpen(false)} className="mt-5 w-full rounded-lg bg-slate-900 py-3 text-[10px] font-extrabold uppercase tracking-wider text-white">Close</button></div></div>}
      {editorOpen && <div className="fixed inset-0 z-40 flex items-center justify-center bg-slate-950/70 p-4" role="dialog" aria-modal="true"><div className="flex max-h-[calc(100vh-32px)] w-full max-w-[1100px] flex-col overflow-hidden rounded-2xl bg-slate-50 shadow-2xl"><header className="flex items-center justify-between border-b border-slate-800 bg-slate-950 px-5 py-3 text-white"><div className="flex items-center gap-3"><button onClick={() => setEditorOpen(false)} aria-label="Close framing editor" className="rounded-md bg-slate-900 p-2 text-slate-400"><ArrowLeft className="size-4" /></button><div><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-teal-300">Capture review</p><h3 className="text-sm font-extrabold">Reframe &amp; straighten</h3></div></div><div className="flex items-center gap-2"><button onClick={() => { setAngle(0); setRatio("Original"); }} className="rounded-md border border-slate-700 px-3 py-2 text-[10px] font-extrabold uppercase tracking-wider text-slate-300"><RotateCcw className="mr-1 inline size-3.5" />Reset</button><button onClick={() => setSaved(true)} className="rounded-md bg-teal-500 px-4 py-2 text-[10px] font-extrabold uppercase tracking-wider text-slate-950"><Check className="mr-1 inline size-3.5" />Save edit</button></div></header><div className="flex min-h-0 flex-1"><div className="flex min-w-0 flex-1 items-center justify-center bg-[#d8e0e1] p-7"><div className={`relative w-full max-w-[680px] overflow-hidden rounded-sm bg-slate-900 shadow-2xl ${ratio === "Square" ? "aspect-square" : ratio === "4 : 5" ? "aspect-[4/5]" : "aspect-[16/10]"}`}><img src={current.image} alt="Focused framing preview" className="h-full w-full object-cover" style={{ transform: `rotate(${angle}deg)` }} /><div className="pointer-events-none absolute inset-0 border border-white/80"><div className="absolute inset-x-0 top-1/3 border-t border-white/35" /><div className="absolute inset-x-0 top-2/3 border-t border-white/35" /><div className="absolute inset-y-0 left-1/3 border-l border-white/35" /><div className="absolute inset-y-0 left-2/3 border-l border-white/35" /></div><span className="absolute bottom-3 left-3 rounded bg-slate-950/70 px-2 py-1 font-mono text-[9px] text-white">PREVIEW ONLY · ORIGINAL UNTOUCHED</span></div></div><aside className="w-[270px] shrink-0 overflow-y-auto border-l border-slate-200 bg-white p-5"><p className="text-[10px] font-extrabold uppercase tracking-[.18em] text-slate-400">Edit controls</p><h4 className="mt-1 text-base font-extrabold">Make a small correction</h4><div className="mt-6 space-y-6"><div><label className="mb-2 flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-widest text-slate-500"><Crop className="size-3.5 text-teal-600" />Aspect ratio</label><select value={ratio} onChange={(event) => setRatio(event.target.value)} className="w-full rounded-md border border-slate-200 bg-slate-50 px-3 py-2.5 text-xs font-bold"><option>Original</option><option>4 : 5</option><option>Square</option></select></div><div><div className="mb-2 flex justify-between"><label className="flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-widest text-slate-500"><SlidersHorizontal className="size-3.5 text-teal-600" />Straighten</label><span className="font-mono text-[10px] text-teal-700">{angle > 0 ? "+" : ""}{angle}°</span></div><input aria-label="Straighten angle" type="range" min="-8" max="8" value={angle} onChange={(event) => setAngle(Number(event.target.value))} className="w-full accent-teal-600" /></div><div className="rounded-lg border border-teal-200 bg-teal-50 p-3 text-xs leading-relaxed text-teal-900"><ShieldCheck className="mb-1 size-4 text-teal-700" /><strong>Safe edit.</strong> Camera original and RAW stay untouched.</div><button onClick={() => setAngle((value) => value + 90)} className="w-full rounded-md border border-slate-200 py-2.5 text-[10px] font-extrabold uppercase tracking-wider text-slate-600"><RotateCw className="mr-1 inline size-3.5" />Rotate right</button></div></aside></div></div></div>}
      {saved && <div role="status" className="fixed bottom-5 right-5 z-50 flex items-center gap-2 rounded-lg border border-teal-300 bg-slate-950 px-4 py-3 text-xs font-bold text-white shadow-xl"><Check className="size-4 text-teal-300" />Reframe saved to review copy</div>}
    </main>
  );
}